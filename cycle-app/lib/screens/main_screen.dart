import 'package:flutter/material.dart';
import 'login_page.dart';

class MainScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return LoginPage();  // 로그인 페이지를 메인 화면으로 사용
  }
}